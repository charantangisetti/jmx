apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "soapconnector.fullname" . }}-test-connection"
  labels:
{{ include "soapconnector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "soapconnector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
