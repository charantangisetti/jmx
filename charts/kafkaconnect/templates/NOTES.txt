export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "kafkaconnect.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
echo "use http://127.0.0.1:8083 to connect with Kafka connect cluster Rest API"
kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8083:8083 