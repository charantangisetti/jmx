apiVersion: v1
kind: Service
metadata:
  name: {{ include "actionmanager.fullname" . }}
  labels:
    {{- include "actionmanager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "actionmanager.selectorLabels" . | nindent 4 }}
