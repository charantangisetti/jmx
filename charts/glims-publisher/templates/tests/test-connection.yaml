apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "glims-publisher.fullname" . }}-test-connection"
  labels:
    {{- include "glims-publisher.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "glims-publisher.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
