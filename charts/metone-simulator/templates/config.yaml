apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "metone-simulator.fullname" . }}-config
data:
  {{ if .Values.mqtt_client_id -}}
  mqtt_client_id: {{ .Values.mqtt_client_id | quote }}
  {{- else -}}
  mqtt_client_id: "METONE_SIM_DH_001"
  {{ end }}