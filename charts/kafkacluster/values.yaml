entityOperator:
  topicOperator: {}
  userOperator: {}
kafka:
  config:
    auto.create.topics.enable: false
    log.message.format.version: "2.8"
    offsets.topic.replication.factor: 3
    transaction.state.log.min.isr: 2
    transaction.state.log.replication.factor: 3
  listeners:
    - name: plain
      port: 9092
      type: internal
      tls: false
    - name: tls
      port: 9093
      type: internal
      tls: true
    # plain: {}
    # tls: {}
  replicas: 3
  storage:
    deleteClaim: false
    size: 10Gi
    type: persistent-claim
  version: 3.9.0
  # JMX configuration with SSL
  jmxOptions:
    authentication:
      type: password
  jvmOptions:
    "-Xmx": "2G"
    "-Xms": "2G"
    "-D":
      "com.sun.management.jmxremote": "true"
      "com.sun.management.jmxremote.port": "9999"
      "com.sun.management.jmxremote.rmi.port": "9999"
      "com.sun.management.jmxremote.ssl": "true"
      "com.sun.management.jmxremote.registry.ssl": "true"
      "com.sun.management.jmxremote.ssl.need.client.auth": "true"
      "javax.net.ssl.keyStore": "/opt/kafka/certificates/jmx-keystore.jks"
      "javax.net.ssl.keyStorePassword": "${JMX_KEYSTORE_PASSWORD}"
      "javax.net.ssl.trustStore": "/opt/kafka/certificates/jmx-truststore.jks"
      "javax.net.ssl.trustStorePassword": "${JMX_TRUSTSTORE_PASSWORD}"
  # Metrics configuration for Prometheus JMX Exporter
  metricsConfig:
    type: jmxPrometheusExporter
    valueFrom:
      configMapKeyRef:
        name: kafka-metrics
        key: kafka-metrics-config.yml
strimzi-kafka-operator:
  watchAnyNamespace: true
zookeeper:
  replicas: 3
  storage:
    deleteClaim: false
    size: 10Gi
    type: persistent-claim
  # JMX configuration with SSL for Zookeeper
  jmxOptions:
    authentication:
      type: password
  jvmOptions:
    "-Xmx": "1G"
    "-Xms": "1G"
    "-D":
      "com.sun.management.jmxremote": "true"
      "com.sun.management.jmxremote.port": "9998"
      "com.sun.management.jmxremote.rmi.port": "9998"
      "com.sun.management.jmxremote.ssl": "true"
      "com.sun.management.jmxremote.registry.ssl": "true"
      "com.sun.management.jmxremote.ssl.need.client.auth": "true"
      "javax.net.ssl.keyStore": "/opt/kafka/certificates/jmx-keystore.jks"
      "javax.net.ssl.keyStorePassword": "${JMX_KEYSTORE_PASSWORD}"
      "javax.net.ssl.trustStore": "/opt/kafka/certificates/jmx-truststore.jks"
      "javax.net.ssl.trustStorePassword": "${JMX_TRUSTSTORE_PASSWORD}"

# JMX SSL Certificates
# Generate these using scripts/generate-jmx-certificates.sh
jmxCertificates:
  # Base64 encoded keystore file
  keystore: ""
  # Base64 encoded truststore file  
  truststore: ""

# JMX Passwords
jmxPasswords:
  keystorePassword: ""
  truststorePassword: ""
