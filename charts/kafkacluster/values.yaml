entityOperator:
  topicOperator: {}
  userOperator: {}
kafka:
  config:
    auto.create.topics.enable: false
    log.message.format.version: "2.8"
    offsets.topic.replication.factor: 3
    transaction.state.log.min.isr: 2
    transaction.state.log.replication.factor: 3
  listeners:
    - name: plain
      port: 9092
      type: internal
      tls: false
    - name: tls
      port: 9093
      type: internal
      tls: true
    # plain: {}
    # tls: {}
  replicas: 3
  storage:
    deleteClaim: false
    size: 10Gi
    type: persistent-claim
  version: 3.9.0
  # JMX configuration - using password auth with Strimzi
  jmxOptions:
    authentication:
      type: password
  jvmOptions:
    "-Xmx": "2G"
    "-Xms": "2G"
    "-XX":
      "UseG1GC": "true"
      "MaxGCPauseMillis": "20"
    javaSystemProperties:
      - name: "javax.net.ssl.keyStore"
        value: "/mnt/jmx-certificates/jmx-keystore.jks"
      - name: "javax.net.ssl.keyStorePassword"
        value: "${JMX_KEYSTORE_PASSWORD}"
      - name: "javax.net.ssl.trustStore"
        value: "/mnt/jmx-certificates/jmx-truststore.jks"
      - name: "javax.net.ssl.trustStorePassword"
        value: "${JMX_TRUSTSTORE_PASSWORD}"
      - name: "com.sun.management.jmxremote.ssl.config.file"
        value: "/mnt/jmx-ssl-config/jmx.ssl.properties"
  # Metrics configuration for Prometheus JMX Exporter
  metricsConfig:
    type: jmxPrometheusExporter
    valueFrom:
      configMapKeyRef:
        name: kafka-metrics
        key: kafka-metrics-config.yml
strimzi-kafka-operator:
  watchAnyNamespace: true
zookeeper:
  replicas: 3
  storage:
    deleteClaim: false
    size: 10Gi
    type: persistent-claim
  # JMX configuration - using password auth with Strimzi
  jmxOptions:
    authentication:
      type: password
  jvmOptions:
    "-Xmx": "1G"
    "-Xms": "1G"
    "-XX":
      "UseG1GC": "true"
    javaSystemProperties:
      - name: "javax.net.ssl.keyStore"
        value: "/mnt/jmx-certificates/jmx-keystore.jks"
      - name: "javax.net.ssl.keyStorePassword"
        value: "${JMX_KEYSTORE_PASSWORD}"
      - name: "javax.net.ssl.trustStore"
        value: "/mnt/jmx-certificates/jmx-truststore.jks"
      - name: "javax.net.ssl.trustStorePassword"
        value: "${JMX_TRUSTSTORE_PASSWORD}"
      - name: "com.sun.management.jmxremote.ssl.config.file"
        value: "/mnt/jmx-ssl-config/jmx.ssl.properties"

# JMX SSL Certificates
# Generate these using scripts/generate-jmx-certificates.sh
jmxCertificates:
  # Base64 encoded keystore file
  keystore: ""
  # Base64 encoded truststore file  
  truststore: ""

# JMX Passwords
jmxPasswords:
  keystorePassword: ""
  truststorePassword: ""
